seed: 42

data:
  demo_npz_path: "data_demo/demo_36x36.npz"

split:
  test_size: 0.2

features:
  method: "svd"
  flatten: false
  use_pca: false

svd:
  top_k: 20

optuna:
  n_trials: 50
  cv_folds: 5
  scoring: "f1"

xgb:
  tree_method: "hist"
  nthread: 1

outputs:
  dir: "outputs/demo_svd"
